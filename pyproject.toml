[project]
name = "audit-agent"
version = "0.2.0"
description = "Repo-hosted audit agent for question generation and answer validation."
requires-python = ">=3.11"

[tool.audit-agent]
report-version = "0.2.0"
